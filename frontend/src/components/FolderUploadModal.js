import * as React from 'react';

export default function FolderUploadModal({ setModalOpen }) {
  return (
    <div className="modal-wrapper">
      <div className="modal-button-group">
        <h1>Upload files to a folder</h1>
      </div>
    </div>
  );
}
